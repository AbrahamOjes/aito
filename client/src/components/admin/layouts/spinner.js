import React, { Fragment } from "react";
import Spinner from "../../../img/Spinner.svg";

export default () => (
  <Fragment>
    <img className="center-pos" src={Spinner} alt="loading...." />
  </Fragment>
);
